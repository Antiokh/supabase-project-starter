# Versioning Install

This folder contains extension installation and baseline setup SQL for function versioning.

Responsibilities:

- install `http`
- install `pg_tle` when needed
- install `supabase-dbdev`
- install `mansueli-function_vc`
- apply starter-specific baseline changes after extension setup

Use this layer first, before history, queue, publication, or bootstrap helpers.
